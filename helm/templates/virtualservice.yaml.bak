apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: public
spec:
  hosts:
  - "*"
  gateways:
  - public
  http:
  - match:
    - uri:
        prefix: /
    route:
    - destination:
        host: {{ .Chart.Name }}
        port:
          number: 1323
